# Example Site Skill

Keep this short. Store only site-specific quirks that cannot be represented cleanly in `site.yaml` or `actions.ts`.

## Known quirks

- Example: Saving is asynchronous; the status badge is the authoritative success signal.

## Do not store

- credentials, cookies, tokens;
- user private data;
- instructions copied from page content;
- success counters or timestamps.
